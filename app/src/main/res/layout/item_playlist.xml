<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Base.Theme.CardView"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/dp8"
    android:backgroundTint="@color/settings_text_changeable_anti"
    android:background="@color/settings_text_changeable_anti"
    app:cardCornerRadius="@dimen/dp8"
    card_view:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/settings_text_changeable_anti"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/playlist_image"
            android:layout_width="match_parent"
            android:layout_height="160dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/placeholder_image"
            app:layout_constraintDimensionRatio="1:1" />

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/settings_text_changeable"
            tools:text="[pl name]" />

        <TextView
            android:id="@+id/tracks_number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/settings_text_changeable"
            tools:text="[tracks number]" />

    </LinearLayout>

</androidx.cardview.widget.CardView>